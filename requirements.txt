# Direct dependencies for the TCGA site-confounding pipeline.
# Curated (not a full `pip freeze`) so it installs cleanly on Linux servers —
# a mac freeze pulls in mac-only packages (e.g. appnope) that break pip on Linux.
# Requires Python 3.12 (pymc 6.x / arviz 1.x are 3.12+). gdc-client must be on PATH.

# core scientific
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
requests==2.34.2
openpyxl==3.1.5          # read the TCGA-CDR .xlsx

# Bayesian modelling
pymc==6.0.1
pytensor==3.0.5
nutpie==0.16.10          # Rust NUTS sampler
numba==0.65.1            # nutpie backend
arviz==1.2.0

# IO / data
pyarrow==24.0.0          # parquet
h5netcdf==1.8.1          # netCDF trace IO

# gene panels / batch correction
gseapy==1.2.1            # MSigDB Hallmark gene sets
inmoose==0.9.1           # pyComBat (ComBat sensitivity)

# notebooks (optional on a headless server)
ipykernel==7.3.0
